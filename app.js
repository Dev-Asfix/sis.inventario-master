const Usuario = require('./Usuario'); // Importar la clase Usuario desde el archivo Usuario.js

const usuario = new Usuario(); // Crear una nueva instancia de la clase Usuario y asignarla a la variable 'usuario'

// Llamar al método para listar usuarios
usuario.listarUsuarios(); // Invocar el método listarUsuarios en la instancia 'usuario' para obtener y mostrar la lista de usuarios
